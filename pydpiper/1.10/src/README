#Needs pyro pyro4 networkx pytest pygraphviz installed in anaconda (via pip or conda)

wget https://github.com/mfriedel/pydpiper/archive/v1.10.tar.gz
tar -xzf v1.10.tar.gz
module load anaconda/2.1.0
python setup.py install --prefix ../../build

